SELECT C.CATEGORYNAME, SUM(D.QUANTITY) AS SALE_QUANTITY , SUM(P.PRICE * D.QUANTITY) AS TOTAL_SALE
FROM PRODUCTS AS P, ORDERDETAILS AS D, CATEGORIES AS C
WHERE D.PRODUCTID = P.PRODUCTID and
P.CATEGORYID = C.CATEGORYID
GROUP BY P.CATEGORYID HAVING SUM(D.QUANTITY) >= 2000;