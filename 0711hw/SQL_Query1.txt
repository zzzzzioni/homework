SELECT C.CUSTOMERNAME, SUM(P.PRICE * D.QUANTITY) AS TOTAL_PRICE 
FROM PRODUCTS AS P, ORDERDETAILS AS D, ORDERS AS O, CUSTOMERS AS C 
WHERE D.PRODUCTID = P.PRODUCTID and 
O.ORDERID = D.ORDERID and 
O.CUSTOMERID = C.CUSTOMERID 
GROUP BY O.CUSTOMERID;